# =============================================================================
# Unified Activity:State Platform - Environment Configuration
# =============================================================================
# Copy this file to .env and customize for your environment.
# All values shown are sensible defaults for development.
#
# For production deployment, see DEPLOYMENT.md for recommended settings.
# =============================================================================

# -----------------------------------------------------------------------------
# Backend (Python/Jones Framework)
# -----------------------------------------------------------------------------

# Environment mode: development | production | testing
JONES_ENV=development

# Hardware acceleration: auto | cuda | metal | cpu
# - auto: Automatically detect best available (recommended)
# - cuda: Force NVIDIA GPU (requires CUDA toolkit)
# - metal: Force Apple Silicon GPU (macOS only)
# - cpu: Force CPU-only mode
JONES_DEVICE=auto

# Logging verbosity: DEBUG | INFO | WARNING | ERROR
JONES_LOG_LEVEL=INFO

# API server configuration
JONES_API_HOST=0.0.0.0
JONES_API_PORT=8000

# TDA Pipeline settings (optional, defaults are usually fine)
# JONES_TDA_EMBEDDING_DIM=3
# JONES_TDA_MAX_DIMENSION=1

# SANS/MoE settings (optional)
# JONES_MOE_NUM_EXPERTS=6
# JONES_MOE_LORA_RANK=8

# -----------------------------------------------------------------------------
# Frontend (React/Vite)
# -----------------------------------------------------------------------------

# Environment mode
NODE_ENV=development

# Backend API URL (where the Python server is running)
VITE_API_URL=http://localhost:8000

# WebSocket URL for real-time streaming (usually same as API)
VITE_WS_URL=ws://localhost:8000

# -----------------------------------------------------------------------------
# Production Deployment (Railway/Cloud)
# -----------------------------------------------------------------------------
# These are typically set by the hosting platform automatically.
# Uncomment and set manually only if deploying to a custom environment.

# PORT=8000

# For Railway.app deployment:
# JONES_ENV=production
# JONES_DEVICE=cpu
# VITE_API_URL=https://your-backend.railway.app

# -----------------------------------------------------------------------------
# Optional: External Data Sources
# -----------------------------------------------------------------------------
# Configure these if connecting to external data providers.

# Market Data (finance domain)
# JONES_MARKET_DATA_PROVIDER=alpaca
# ALPACA_API_KEY=your_key_here
# ALPACA_SECRET_KEY=your_secret_here

# Drilling Data (drilling domain)
# JONES_WITSML_ENDPOINT=https://your-witsml-server.com
# JONES_WITSML_USERNAME=username
# JONES_WITSML_PASSWORD=password

# -----------------------------------------------------------------------------
# Optional: Monitoring & Observability
# -----------------------------------------------------------------------------

# Enable metrics export
# JONES_METRICS_ENABLED=true
# JONES_METRICS_PORT=9090

# Sentry error tracking (production)
# SENTRY_DSN=https://your-sentry-dsn
